---
import Base from '../layouts/Base.astro';
import Hero from '../components/scenes/Hero.astro';
import WhyNow from '../components/scenes/WhyNow.astro';
import Stake from '../components/scroll-Stake-alt.astro';
import PilotsScene from '../components/scenes/PilotsScene.astro';
import ForumScene from '../components/scenes/ForumScene.astro';
import PeopleScene from '../components/scenes/PeopleScene.astro';
---
{/* Six beats. No two adjacent scenes land at the same weight, on the same
     ground, or in the same composition:
       Hero      photograph · poster left, named ledger low     100svh
       WhyNow    cream · narrow left column, vertical rule, claim right
       Stake     photograph · ONE sentence, set RIGHT               108svh
       Pilots    ink-900 · the long ledger, flush left
       Forum     photograph · THE HELD SCENE — pinned, 2.9 viewports
       People    ink-900 head, then a full-bleed ink-800 WALL of names

     THREE ARMATURES. The site's default move — micro label at the left rail,
     hairline ruled the full frame, block hung beneath — belongs to Hero,
     Pilots and Forum. WhyNow turns it ninety degrees (the division is
     vertical, the major column is the right one). Stake abandons the
     armature entirely: no label, no rule, no counter, no eyebrow, one
     sentence set right.

     THE METHOD SCENE IS GONE, and this is the seventh beat that was here.
     It set `method` — the same three-item array, the same three titles and
     the same three captions — that /institute/ sets as a numbered left-ranged
     ledger. The same content, twice, in two grammars, and the homepage had
     the weaker one: centre-set, the only centred block on a site whose whole
     grammar is a left rail (a left-edge histogram of this page put 28 text
     elements at exactly x=51, and none of them were in that scene), with all
     three titles and two of the three captions wrapping to two lines that
     set on one line in the ledger. A reader who follows this page's own
     INSTITUTE link meets the identical three steps set properly.

     It goes rather than gets left-railed, and the reason is that
     left-railing it would have made the duplication WORSE: matched to the
     ledger's grammar, the two would have been the same object rendered
     twice. Removing it closes both defects with one move, and it is the move
     the brief already asked for. refs/BRIEF.md's site map gives Home
     "mission line, four pilots as cards, Lion Forum as one scene, one CTA"
     — no method explainer — and gives Institute "what we do and how we work,
     as a diagram or three beats", which is exactly what /institute/ already
     carries, in both of the forms named, one click away.

     Nothing factual left with it. The three steps are on /institute/; the
     "3–12 months" figure is /institute/'s own Horizon row, /pilots/'s
     opening, and every pilot's `horizon`. And the scene was a legend for a
     table that describes itself: Pilots, one screen below, states the same
     sequence concretely — a problem a family can name, who builds it and who
     runs it, one measurable outcome each — as problem / approach / goal on
     four cards.

     Stake was described here as the hinge between Method (abstract) and
     Pilots (concrete). It still hinges, between WhyNow (the claim) and
     Pilots (the answer), which is the shorter argument.

     `src/components/scenes/MethodScene.astro` is deleted rather than left
     unimported. This repo has already shipped one scene that existed only in
     a file nothing built (`HeldScene` in `_lab.astro`, zero held scenes in
     `dist/` for four waves); an orphaned component is that trap with the
     safety off.

     THE EXCEPTION, SPENT ONCE. Stake is 108svh ON DESKTOP — taller than the
     viewport it is read in, the only element on the site that is. It exists
     because four documentary photographs were in the repo, referenced in the
     DOM, and rendered to nobody: they lived inside a 224x280px cursor plate
     that never loaded and was display:none on a phone, and the 1,059px
     Pilots ledger below held no image at all. That plate is now GONE — an
     uncredited, aria-hidden, desktop-only ornament that put identifiable
     people (one of them a child) under a moving cursor to no argument; the
     ledger's hover state is ink only, and /pilots/ shows the photographs at
     size with credits. The 108svh exception is desktop-only for the same
     class of reason: on a 390px screen the tall slot filled the whole
     viewport with two faces and pushed the sentence below the fold, so the
     phone gets a band that holds the picture and its line in one screen. One of them is now the largest object
     on the page, on every viewport. It is also the hinge between WhyNow
     (the claim) and Pilots (the answer) — it used to hinge Method to Pilots,
     which were consecutive and opened with the identical three-element
     overture; Method is gone and the two it now separates were never that
     pair.

     TONAL PACING, MEASURED ON THE SHIPPED PAGE. Wave 4 opened with "79% of
     the page inside a 5.7-point L* band". `tools/tone-meter.mjs`, run against
     the final build — real scroll positions, gutter columns, 20px rows:

         8,528px · span L* 2.5 -> 93.9 · 27 grounds, 8 of them longer than a
         third of a viewport · no single band holds more than 13% of the
         scroll · 52% of the scroll within 9 L* of the navy floor

         hero photograph     200– 780   L* 29 – 44
         cream WhyNow        920–1420   L* 93.8
           Stake photograph   2880–3120   L* 24
         Pilots ledger      3140–4260   L*  3.7   <- the one flat run left
         THE HELD FORUM     4740–6220   L* 40 -> 69.5 -> 4, inside one scene
         board wall         7460–7980   L* 14.5

     The Pilots ledger is still 1,120px of one value; this file does not own
     it. The hold is what now answers it — the ledger's flat navy runs
     straight into a picture that opens out of that exact value.
     GROUND CLASSES SPENT: two — .on-cream once (WhyNow) and .on-panel once
     (the board wall). Under the ceiling in tokens.css. Removing the Method
     panel removed one 340px tonal event; the cream now runs straight into
     the Stake photograph, which is a hard tonal edge and not a seam — the
     halved bottom pad Method carried existed because a navy panel above a
     navy-ground photograph reads as a seam at near-identical value, and
     cream against that photograph is the opposite case. Re-measured below.

     The People wall runs flush into the footer, so the page ends on a hard
     tonal edge rather than on 290px of empty navy.

     SEAMS PER FRAME, AND WHY `min-height: 100svh` IS NOT THE ANSWER.
     The wave-31 comparison — the first whose captures actually traversed
     both documents — scored 3-3 and named one gap: that our sections are
     shorter than the viewport, so nearly every frame of a scroll contains a
     section boundary, and the reference's contain none. It proposed
     `min-height: 100svh` with content centred on the short bands, claiming
     that would put eight of eleven offending frames onto one subject.

     THE COUNT IS REAL AND IT IS WORSE THAN QUOTED. Thirty settled frames at
     the offsets `capture-motion.mjs` records, sections read off the live DOM,
     "visible" = overlapping the band below the fixed nav (837px desktop,
     786px mobile):

         desktop  25/30 frames show >1 section · 19/30 show two at >=15% each
         mobile   24/30                        · 20/30 · and 5 show THREE

     Scaled to the judge's twenty that is 12.7, not 11. Confirmed, not refuted.

     THE PROPOSED FIX WAS BUILT AND MEASURED, ON ONE SECTION, AND IT FAILS.
     `min-height: 100svh; display: grid; align-content: center` on `.why`:

         page length   6608 -> 7007px   (+399, +6.0%)
         frames        25/30 -> 23/30 any · 19/30 -> 18/30 substantive

     +399px of document bought TWO frames. Not eight of eleven. Extrapolated
     over eight sections that is a homepage half again as long for a quarter
     of the frames, on a site that WINS information density in the same
     scoring.

     THE ARITHMETIC SAYS WHY, AND IT IS NOT A TUNING PROBLEM. A section of
     height H is seen alone only across `max(0, H - B)` of scroll, B being the
     band below the nav. Setting H = 100svh makes that `B_viewport - B_band` =
     the height of the nav, 63px, while adding 400px of scroll. It moves the
     seam-free RATIO the wrong way per pixel spent. Summed over the shipped
     page: 1444px of 5708 scrollable is seam-free, 25.3% — and 918 of that
     1444, sixty-four per cent of it, comes from ONE section, the held Forum
     scene, which is 1755px because it is PINNED. That is the reference's
     actual mechanism, and we already own it.

     To reach the reference's near-zero seam rate by height alone, with seven
     bands and a 900px viewport, every band must be ~7,200px: a homepage of
     ~50,000px, 7.6x this one. Even settling for HALF the frames clean needs
     ~1,550px a band and a 10,800px page, +64%. There is no setting of this
     dial that is both cheap and effective.

     AND THE FIX CONTRADICTS THIS SITE'S OWN HELD-SPACE RULE. Ran it on the
     probe: green, 0 holes — but it grew the site from 41 bands to 45 and put
     a 205px desktop / 268px mobile field of dead cream under "OUR ANSWER",
     where the baseline homepage's largest band is 131px (cross 72) desktop
     and 70px mobile. 268px is larger than the /404 band wave 22 cropped as
     the largest hole on the site. Centring content in a viewport-tall box
     manufactures held space at both ends of it; that is what padding a scene
     to the fold DOES. It also cuts the vertical rule that is WhyNow's whole
     armature — the grid track no longer spans the section, so the division
     stops a third of the way down and reads as the edge of a box, which is
     the exact failure the note beside `.why__in` was written to prevent.

     THE OTHER THREE ROUTES OUT, ARGUED AND REFUSED.
       · Full-bleed grounds that hide the seam. This one is backwards. On
         this site the seam IS a change of ground of >=11.5 L*, and by
         clause 1 of the held-space rule a change of ground of >=11.5 L* is
         precisely what CLOSES a band. Make the join invisible and every band
         at it becomes, by our own measurement, a hole. The judge's frame 10
         is the proof in the other direction: photo edge, 173px of navy, then
         the pilots eyebrow — the navy is not machinery showing, it is the
         air, and it is air only because the edge above it is hard.
       · scroll-snap. The Pilots ledger is 1,096px in a 900px viewport; a
         reader cannot reach row 04 without being thrown to the next scene.
         It also fights the pinned held scene, which owns the scroll it is on.
       · Fewer, larger sections. Correct in principle and already spent: the
         Method scene came out of this page for that reason, six beats not
         seven. Doing it again means cutting a pilot, the board, or the
         funding model — trading the two criteria this scoring says we win
         for the one it says we lose.

     SO: NOT DONE, DELIBERATELY. The seams on this page are a document of
     seven pages of real content changing subject, and every one of them is
     a change of ground the held-space gate reads as composition. The one
     place this site buys a seam-free run it buys honestly, by pinning a
     scene for 2.9 viewports. If a later wave wants more of them, the move is
     another PIN, not another 100svh — and it needs a scene worth 1,800px of
     scroll, not a band padded to the fold.

     THE STAKE SHIPS AS VERSION (b). `scroll-Stake-alt.astro` is imported
     above and `scroll-Stake.astro` is not. The desktop composition — the
     one a critic named — is byte-identical; what changes is the sentence
     ("The test is ordinary life." for "For the median family.") and the
     phone crop. Both were editorial, not aesthetic: the old line printed a
     statistician's abstraction over an identifiable woman in a U.S.
     Department of Agriculture photograph, and the old phone crop centred her
     face, in focus, at full width. (b)'s own phone crop was measured again
     in round 2 and moved further — 44%/86% at zoom 1.3 — because at 46%/66%
     her full profile was still across the top third of the frame at the head
     of the scene. Screenshots of both versions: progress/shots/
     w4r2-b-shipped/ and w4r2-a-original/.

     ONE SCENE ON THIS PAGE IS PINNED, and it is the Forum. `HeldScene` may
     be spent exactly once site-wide; `[data-hold]` appears once in the whole
     of `dist/`, here. The two candidates this file used to argue over are
     still not it, for the reasons they were rejected:

       Hero    holds the four 2026 partners BY NAME inside the first screen.
               They are the only credential this institute has. Pinning the
               hero puts that ledger two and a half viewport heights below
               the fold, behind a hold, on the front door.
       Stake   is the composition a critic named, and it works because one
               sentence is ranged right into the patch of wall the crop was
               chosen to leave, at the height it is empty. HeldScene sets
               its beats in a band at the FOOT of the frame. Pinning this
               scene does not enhance that composition; it replaces it.

     This file used to send the hold to /forum/ on the reference's nomination
     (refs/MOTION-FINDINGS.md: "most likely the Forum, whose photograph is the
     most evocative asset the Institute has"). It went there and it did not
     ship: `HeldScene` existed only in `src/pages/_lab.astro`, which is
     underscore-prefixed and never built, so the site shipped ZERO held scenes
     and the finding was unspent. /forum/ cannot host it — that page's copy is
     fully spent across masthead, claim, ledger and close, and every candidate
     beat there duplicates a section a thousand pixels above it.

     The Forum SCENE, on this page, can: every string the hold needs was
     already in it. See components/scenes/ForumScene.astro. It is also the
     right place tonally — it lands at the end of the Pilots ledger, which is
     the one event-free run this page has left, and swings the gutter from
     L* 3.7 to L* 69.5 and back inside one scene. */}
<Base>
  <Hero />
  <WhyNow />
  <Stake />
  <PilotsScene />
  <ForumScene />
  <PeopleScene />
</Base>
